# {{ package.name }} API Reference

Package: {{ package.name }} v{{ package.version }}
Import: `import {{ package.import_name }}`
{% if package.homepage %}
Homepage: {{ package.homepage }}
{% endif %}

---

{% for tool in tools %}
## `{{ tool.qualname }}`

{{ (tool.long_description or tool.description) | sanitize }}

### Signature

```python
{{ tool.qualname }}{{ tool | tools_signature }}
```

### Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
{% for param in tool.parameters %}
| `{{ param.name }}` | `{{ param.type_str or 'any' }}` | {{ 'Yes' if param.required else 'No' }} | {{ param.default or '-' }} | {{ (param.description | sanitize) or '-' }} |
{% endfor %}

{% if tool.output_hint and tool.output_hint != 'unknown' %}
### Returns

`{{ tool.output_hint }}`
{% endif %}

{% if tool.example %}
### Example

```python
{{ tool.example }}
```
{% endif %}

### JSON Schema

```json
{{ tool.input_schema | tojson(indent=2) }}
```

---

{% endfor %}

*Generated by pip-skill v{{ pip_skill_version }} on {{ timestamp }}*
